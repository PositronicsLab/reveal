#include <Reveal/core/system.h>

#include <string>
#include <stdio.h>
#include <sstream>

int main( int argc, char* argv[] ) {
/*
  std::string temp_path;
  if( !make_temp_dir( temp_path ) ) {
    printf( "Failed to generate temp path\n" );
    return 1;
  }
*/
/*
  if( !get_directory( temp_path ) ) {
    printf( "Failed to create temp path\n" );
    return 1; 
  }
*/
//  std::string source_dir = "/home/james/testdir";
  
//  std::string target_dir = "/home/james/testdir2";

  //get_directory( target_dir );

//  copy_directory( source_dir, target_dir );
  //clean_directory( target_dir );
  //remove_directory( target_dir );
  
  //copy_directory( source_dir, temp_path );

  //remove_directory( target_dir );
  //remove_directory( temp_path );

  return 0;
}
